
var exports = module.exports = {

	/**
	 * Holds the lowest interesting log levels for each transport.
	 * @type {Object}
	 */
	lowest: {
		/**
		 * The lowest log level that should be printed to console.
		 * @type {String}
		 */
		console: 	'debug',

		/**
		 * The lowest log level that should be written to the file.
		 * @type {String}
		 */
		file: 		'info',
	}
};
